import { useEffect, useMemo, useState } from "react";
import { BookOpen, Plus, Search, Trash2 } from "lucide-react";
import Header from "../Component/Header"; import Sidebar from "../Component/Sidebar"; import api from "../services/api";
type Resource={id:number;title:string;content:string;type:string;tags:string[]|null;createdAt:string};
export default function Resources(){
    const [items,setItems]=useState<Resource[]>([]);
    const [query,setQuery]=useState("");
    const [show,setShow]=useState(false);
    const [form,setForm]=useState({title:"",content:"",type:"NOTE",tags:""});
    useEffect(()=>{api.get<Resource[]>("/resources").then(r=>setItems(r.data)).catch(()=>setItems([]))},[]);
    const filtered=useMemo(()=>items.filter(i=>`${i.title} ${i.content} ${(i.tags??[]).join(" ")}`.toLowerCase().includes(query.toLowerCase())),[items,query]);
    async function create(e:React.FormEvent){e.preventDefault();const r=await api.post<Resource>("/resources",{...form,tags:form.tags.split(",").map(x=>x.trim()).filter(Boolean)});setItems([r.data,...items]);setForm({title:"",content:"",type:"NOTE",tags:""});setShow(false)}async function remove(id:number){await api.delete(`/resources/${id}`);setItems(items.filter(i=>i.id!==id))}return <div className="min-h-screen bg-slate-50 dark:bg-slate-950"><Header/><div className="flex"><Sidebar/><main className="min-w-0 flex-1 px-5 py-8 md:px-10"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="dev-mono text-xs uppercase tracking-[.25em] text-cyan-600">Knowledge hub</p><h1 className="mt-2 text-3xl font-bold">Resources</h1><p className="mt-2 text-sm text-slate-500 dark:text-slate-400">Keep snippets, notes, and references close to your build.</p></div><button onClick={()=>setShow(true)} className="flex items-center gap-2 rounded-xl bg-slate-950 px-4 py-3 text-sm font-semibold text-white"><Plus size={16}/> New resource</button></div><div className="relative mt-8"><Search className="absolute left-3 top-3 text-slate-400" size={18}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search resources..." className="w-full rounded-xl border border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-900 py-3 pl-10 pr-4 outline-none focus:border-cyan-500"/></div>{filtered.length===0?<div className="mt-8 rounded-3xl border border-dashed border-slate-300 bg-white dark:bg-slate-900 p-12 text-center"><BookOpen className="mx-auto text-cyan-500"/><h2 className="mt-3 font-semibold">No resources yet</h2><p className="mt-2 text-sm text-slate-500 dark:text-slate-400">Save your first snippet, note, or reference.</p></div>:<div className="mt-8 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{filtered.map(i=><article key={i.id} className="rounded-2xl border border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-900 p-5 shadow-sm"><div className="flex justify-between"><span className="dev-mono text-[10px] uppercase text-cyan-600">{i.type}</span><button onClick={()=>remove(i.id)} className="text-slate-300 hover:text-red-500"><Trash2 size={16}/></button></div><h2 className="mt-4 font-semibold">{i.title}</h2><p className="mt-2 line-clamp-4 whitespace-pre-wrap text-sm text-slate-500 dark:text-slate-400">{i.content}</p><div className="mt-4 flex flex-wrap gap-1">{(i.tags??[]).map(t=><span key={t} className="rounded bg-slate-100 dark:bg-slate-800 px-2 py-1 text-[10px] text-slate-500 dark:text-slate-400">#{t}</span>)}</div></article>)}</div>}{show&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/40 p-4"><form onSubmit={create} className="w-full max-w-lg space-y-4 rounded-2xl bg-white dark:bg-slate-900 p-6"><h2 className="text-xl font-bold">New resource</h2><input required placeholder="Title" value={form.title} onChange={e=>setForm({...form,title:e.target.value})} className="w-full rounded-xl border p-3"/><select value={form.type} onChange={e=>setForm({...form,type:e.target.value})} className="w-full rounded-xl border p-3"><option>NOTE</option><option>SNIPPET</option><option>DOCUMENTATION</option><option>REFERENCE</option></select><textarea required rows={7} placeholder="Write or paste your resource..." value={form.content} onChange={e=>setForm({...form,content:e.target.value})} className="w-full rounded-xl border p-3"/><input placeholder="Tags, comma separated" value={form.tags} onChange={e=>setForm({...form,tags:e.target.value})} className="w-full rounded-xl border p-3"/><div className="flex justify-end gap-3"><button type="button" onClick={()=>setShow(false)} className="rounded-xl px-4 py-2 text-sm">Cancel</button><button className="rounded-xl bg-slate-950 px-4 py-2 text-sm font-semibold text-white">Save resource</button></div></form></div>}</main></div></div>}
