import { TrendingUp, Target, Zap } from "lucide-react";
import { useEffect, useState } from "react";
import Header from "../Component/Header"; import Sidebar from "../Component/Sidebar"; import { projectService, type Project } from "../services/projectService";
export default function Progress() { const [projects,setProjects]=useState<Project[]>([]); useEffect(()=>{projectService.getAll().then(setProjects).catch(()=>setProjects([]))},[]); const completed=projects.filter(p=>p.status==="COMPLETED").length; const average=projects.length?Math.round(projects.reduce((a,p)=>a+p.progress,0)/projects.length):0; return <div className="min-h-screen bg-slate-50 text-slate-900"><Header/><div className="flex"><Sidebar/><main className="min-w-0 flex-1 px-5 py-6 md:px-10 md:py-10"><div className="mb-8"><p className="text-sm font-semibold uppercase tracking-[0.25em] text-cyan-600">Progress overview</p><h1 className="text-3xl font-bold tracking-tight">See how your momentum is building</h1></div><div className="grid gap-4 md:grid-cols-3">{[["Projects",projects.length,"Your workspace"],["Average progress",`${average}%`,"Across your projects"],["Completed",completed,"Projects shipped"]].map(([label,value,detail])=><section key={label as string} className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center gap-2 text-cyan-600"><TrendingUp size={18}/><span className="text-sm font-semibold">{label as string}</span></div><p className="mt-4 text-3xl font-bold">{value as string}</p><p className="mt-1 text-sm text-slate-500">{detail as string}</p></section>)}</div><section className="mt-6 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center gap-2 text-cyan-600"><Target size={18}/><h2 className="text-lg font-semibold">Project progress</h2></div>{projects.length===0?<p className="mt-4 text-sm text-slate-500">Create a project to start tracking progress.</p>:<div className="mt-4 space-y-4">{projects.map(p=><div key={p.id}><div className="flex justify-between text-sm"><span>{p.name}</span><b>{p.progress}%</b></div><div className="mt-2 h-2 rounded-full bg-slate-100"><div className="h-2 rounded-full bg-cyan-500" style={{width:`${p.progress}%`}}/></div></div>)}</div>}</section><section className="mt-6 rounded-3xl border border-slate-200 bg-slate-950 p-6 text-white"><div className="flex items-center gap-2 text-cyan-300"><Zap size={18}/><h2 className="text-lg font-semibold">Momentum tip</h2></div><p className="mt-3 max-w-2xl text-sm text-slate-300">Keep your milestones moving and your progress will update automatically.</p></section></main></div></div> }