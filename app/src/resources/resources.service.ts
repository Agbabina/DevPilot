import { Injectable,NotFoundException } from '@nestjs/common'; import { InjectRepository } from '@nestjs/typeorm'; import { Repository } from 'typeorm'; import { Resource } from './resource.entity'; import { CreateResourceDto } from './create-resource.dto'; @Injectable() export class ResourcesService { constructor(@InjectRepository(Resource) private readonly resources:Repository<Resource>){} findAll(ownerId:number){return this.resources.find({where:{ownerId},order:{updatedAt:'DESC'}})} create(ownerId:number,dto:CreateResourceDto){return this.resources.save(this.resources.create({...dto,ownerId}))} async remove(id:number,ownerId:number){const item=await this.resources.findOne({where:{id,ownerId}});if(!item)throw new NotFoundException('Resource not found');await this.resources.remove(item);return {message:'Resource deleted successfully'}} }
